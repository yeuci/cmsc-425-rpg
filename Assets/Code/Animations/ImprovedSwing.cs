using UnityEngine;

public class ImprovedSwing : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {
        //Do some variable control here
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log("Improved Swing Running");
    }
}